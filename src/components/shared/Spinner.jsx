import React from 'react';
import { ClockLoader } from 'react-spinners';

const spinner = () => {
    return (
        <div>
            <ClockLoader />
        </div>
    );
};

export default spinner;